---
import { useTranslations, defaultLang ,type Lang, type TranslationKey } from "../i18n";

const lang = (Astro.currentLocale ?? "es") as Lang;
const t = useTranslations(lang);

const servicios:{
    titulo: TranslationKey;
    texto: TranslationKey;
    destacado?: boolean;
}[] = [
    {titulo: "services.design.title, text:services.design.text"},
    {titulo: "services.i18n.title", "text: services.i18n.text"},
    {titulo: "services.optimization.title", texto: "services.optimization.text"},
]

const claseTarejeta = "relative bg-white border border-slate-200 runded-2xl p-6 shadow-[0_10px_30px_rgba (15,23,42,0.08)]";
---